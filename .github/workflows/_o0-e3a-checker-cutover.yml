name: O0 E3A invalid scratch marker

# INVALID / DO NOT MERGE.
# The independent O0 audit found that source-writing temporary workflows are
# explicitly forbidden by docs/ARCHITECTURE_ACCEPTANCE_PROTOCOL.md. Keep this
# branch inert until PR #678 is superseded by a clean bounded branch.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inert:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse scratch execution
        run: exit 1
